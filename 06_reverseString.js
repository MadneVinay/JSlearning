let reverseString = function(str){
    let word = str.length
    for (let index = word-1; index >= 0; index--) {
        result = str.charAt(index)
        if(result!= " "){
            console.log(result);  
        }
       
    }
console.log("============================");
}
reverseString("Soon i will be Angular IT champ")
reverseString("hard work always pays back")

